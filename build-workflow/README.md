# Deprecated Mirror

This directory is kept only for compatibility with earlier packaging of the repository.

The maintained workflow now lives at the repository root:
- `AGENTS.md`
- `docs/workflows/`
- `setup-codex.sh`

If you are starting fresh, ignore this directory and use the root-level Codex workflow instead.
